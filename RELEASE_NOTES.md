# Список покупок v1.4.6

Публичный релиз Android-приложения и серверной части для самостоятельного размещения.

## Изменения именно в v1.4.6

- На первом экране входа и регистрации Android добавлен чек-бокс "Использовать тестовый сервер".
- При включении чек-бокса автоматически используется `https://rust.bghitech.ru`, поле ручного адреса сервера становится неактивным.
- В настройках Android прямое переключение сервера заменено на безопасную кнопку "Сменить сервер".
- Перед сменой сервера приложение показывает предупреждение, что текущая сессия будет завершена, а аккаунт не переносится автоматически.
- При подтверждении смены сервера очищаются токен, локальная сессия, кэш списков и очередь офлайн-действий, после чего пользователь возвращается на экран входа.
- Настройка тестового сервера хранится отдельно от ручного адреса: при отключении чек-бокса на экране входа приложение возвращается к последнему пользовательскому серверу.
- Исправлено отображение таблицы пользователей в `/admin`: кнопки действий больше не сжимаются в два ряда.
- Версия API поднята до `1.4.6`, чтобы в админке было видно обновление сервера.
- Android version обновлен до `1.4.6`, `versionCode = 29`.

## Изменения именно в v1.4.5

- Исправлена кнопка "Скачать обновление" при ручной проверке в окне "О приложении": теперь она открывает прямую ссылку на APK-файл, а не страницу релиза GitHub.
- Исправлена кнопка "Скачать" в уведомлении о новой версии при запуске: она тоже ведет прямо на APK-файл.
- Версия API поднята до `1.4.5`, чтобы в админке было видно обновление сервера.
- Android version обновлен до `1.4.5`, `versionCode = 28`.

## Изменения именно в v1.4.4

- Исправлено отображение платформы Android-клиента в `/admin/users`: вместо технического фрагмента шаблона показывается нормальное значение, например `android 14`.
- Проверка обновлений Android при запуске больше не блокируется 12-часовым интервалом: при каждом запуске приложение проверяет последний GitHub Release.
- При недоступности GitHub или сети уведомление по-прежнему не показывается.
- Версия API поднята до `1.4.4`, чтобы в админке было видно обновление сервера.
- Android version обновлен до `1.4.4`, `versionCode = 27`.

## Изменения именно в v1.4.3

- PostgreSQL healthcheck в Docker Compose изменён с 10 секунд на 30 секунд.
- Версия API поднята до `1.4.3`, чтобы в админке было видно фактическое обновление сервера.
- Страница `/admin` отдаётся с запретом кеширования, чтобы браузер не показывал старый интерфейс администрирования.
- В админке пользователей отображаются данные Android-клиента: приложение, версия, versionCode, платформа и последний запуск.
- Android version обновлен до `1.4.3`, `versionCode = 26`.

## Изменения именно в v1.4.2-update-check

- Android-приложение при запуске проверяет последний GitHub Release проекта.
- Если доступна новая APK-версия, вверху главного экрана показывается компактное уведомление.
- Кнопка "Скачать" открывает страницу GitHub Release в браузере.
- Кнопка "Скрыть" больше не показывает уведомление для этой же версии; новая версия появится снова.
- Если GitHub, сеть или сервер обновлений недоступны, приложение ничего не показывает и продолжает работать без ошибок.
- APK не скачивается, не устанавливается автоматически и не требует дополнительных разрешений.
- Логика проверки вынесена в отдельные Android-компоненты `UpdateRepository`, `UpdatePreferences`, `UpdateApi` и `VersionComparator`.
- Добавлены JVM-тесты сравнения версий и политики проверки/скрытия обновления.
- Android version обновлен до `1.4.2`, `versionCode = 25`.

## Полный список изменений v1.4.x

- Android-приложение умеет проверять GitHub Releases при запуске и показывать ненавязчивое уведомление о новой APK-версии.
- Админка показывает последнюю версию Android-приложения, с которой пользователь синхронизировался.
- Добавлены административные разделы backend для эксплуатации сервера: пользователи, списки, приглашения, система, логи и диагностика.
- Администратор может отключать и включать пользователей, а также задавать новый пароль пользователю.
- Нельзя отключить последнего активного администратора.
- Отключенный пользователь больше не может войти и использовать уже выданный токен.
- Добавлено архивирование и восстановление списков через admin API.
- Архивные списки не показываются обычному клиенту в `/sync`.
- Добавлен просмотр и отзыв invite-ссылок через admin API.
- Отозванное приглашение больше нельзя принять.
- Добавлены безопасные health endpoints: `/health/live`, `/health/ready`, `/health/db`.
- Добавлен безопасный `/metrics` в JSON-формате без email, паролей и секретов.
- Добавлены CLI-команды `backup`, `restore` и `db-status`.
- Backup по умолчанию не содержит `password_hash`, JWT-секреты и пароли БД.
- Добавлена видимость Alembic-миграций в CLI и `/admin/system`.
- Добавлена in-memory диагностика последних событий сервера.
- Добавлена миграция БД `20260613_0004_ops_admin_fields`.
- Backend tests расширены до 45 тестов.
- Android версия обновлена до `1.4.6`, `versionCode = 29`.

## Полный состав

- Android-приложение для совместных списков покупок.
- Сервер синхронизации на FastAPI и PostgreSQL.
- Docker/GHCR-образы для самостоятельного размещения.
- Первый запуск через `/setup` с обязательным созданием администратора.
- Админские разделы `/admin`, `/admin/status`, `/admin/users`, `/admin/lists`, `/admin/invites`, `/admin/system`, `/admin/logs`, `/admin/diagnostics`.
- Offline queue Android с идемпотентными операциями через `client_operation_id`.
- Health и metrics endpoints для проверки состояния сервера.
- CLI-команды обслуживания: backup, restore, db-status.

## Совместимость

- Старый `/health` сохранен.
- Старые Android-клиенты без `client_operation_id` продолжают работать.
- Offline queue не менялась в этом релизе.
- Роли участников списков `owner/editor/viewer` в этом релизе не добавлялись.
- WebSocket, push-уведомления и Redis не добавлялись.

## Android

- Минимальная версия Android: Android 8.0 Oreo.
- Минимальный API: 26.
- Целевая версия сборки: Android 15, API 35.

## Docker images

```text
ghcr.io/shurshick/shopping-list-api:latest
ghcr.io/shurshick/shopping-list-api:v1.4.6
```

## Проверки

```bash
python -m pytest backend/tests -q
cd android
./gradlew assembleDebug --stacktrace
./gradlew assembleRelease --stacktrace
git diff --check
```

## Файлы релиза

- `shopping-list-android-v1.4.6.apk` - Android-приложение для установки на телефон.
- `shopping-list-server-v1.4.6.zip` - серверная часть, Docker Compose-файлы и инструкции.
- `shopping-list-source-v1.4.6.zip` - полный архив исходного кода.

## Перед обновлением сервера

1. Сделайте backup БД или выполните `python -m app.cli backup --output backup.json` внутри окружения backend.
2. Обновите Docker image до `latest` или `v1.4.6`.
3. Перезапустите контейнер API.
4. Проверьте `/health/ready` и `/admin/system`.

Подробности: `docs/operations.md` и `docs/backup_restore.md`.
